/*export const Logout = ({ logout }) => {
  async function userLogout() {
    fetch(`http://localhost:5000/dashboard/${logout._Id}`);
  }
  function handleLogoutClick() {}
  return (
    <>
      {logout ? (
        <div>
          <button onClick={handleLogoutClick}>Logout</button>
        </div>
      ) : (
        ""
      )}
    </>
  );
};
*/